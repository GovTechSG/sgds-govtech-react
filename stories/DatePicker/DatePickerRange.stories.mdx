import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { DatePicker, Row, Col } from '../../src/index';
import { useState, useRef } from 'react';
import { argTypes } from './argTypes';

<Meta
  title="Components/DatePickerRange"
  component={DatePicker}
  argTypes={{ 
     ...argTypes
  }}
/>

export const DatePickerRangeTemplate = (args) => {
  const initial = { start: new Date(), end: new Date() };
  const [value, setValue] = useState(initial);
  const ref = React.createRef()
  const handleChange = (value) => {
    setValue(value);
    console.log(value);
  };
  const minDate = new Date(2020, 10, 11, 12, 0, 0, 0).toISOString();
  const maxDate = new Date(2020, 10, 13, 12, 0, 0, 0).toISOString();
  return (
    <div style={{ width: '500px' }}>
      <DatePicker value={value} onChange={handleChange} {...args} />
    </div>
  );
};

<Canvas>
  <Story
    name="Multi-Selection Date"
    args={{mode: 'range' }}
    height='500px'
  >
    {DatePickerRangeTemplate.bind({})}
  </Story>
</Canvas>
