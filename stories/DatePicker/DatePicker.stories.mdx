import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { DatePicker, Row, Col } from '../../src/index';
import { useState } from 'react';

<Meta title="Components/DatePicker" component={DatePicker} />

export const DatePickerTemplate = (args) => {
  const [value, setValue] = useState(new Date());
  const handleChange = (value) => {
    setValue(value);
  };
  const minDate = new Date(2020, 10, 11).toISOString()
  const maxDate = new Date(2020, 10, 13).toISOString()
  return (
      <div style={{width: '500px'}}>
        <DatePicker  value={value} onChange={handleChange} {...args}/>
      </div>
  );
};

<Canvas>
  <Story name="Default">{DatePickerTemplate.bind({})}</Story>
</Canvas>


<Canvas>
  <Story name="Multi-Selection Date" args={{multiSelection: true, mode: 'range', dateFormat: 'MM/DD/YYYY'}}>{DatePickerTemplate.bind({})}</Story>
</Canvas>
