import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { DatePicker, Row, Col } from '../../src/index';
import { useState } from 'react';

<Meta title="Components/DatePicker" component={DatePicker} />

export const DatePickerTemplate = (args) => {
  const [value, setValue] = useState(new Date());
  const handleChange = (value) => {
    setValue(value);
    console.log(value)
  };
  const minDate = new Date(2020, 10, 11, 12, 0,0,0).toISOString()
  const maxDate = new Date(2020, 10, 13, 12,0, 0, 0).toISOString()
  return (
      <div style={{width: '500px'}}>
        <DatePicker value={value} onChange={handleChange} {...args} />
      </div>
  );
};

<Canvas>
  <Story name="Default">{DatePickerTemplate.bind({})}</Story>
</Canvas>



<Canvas>
  <Story name="Multi-Selection Date" args={{multiSelection: true, mode: 'range'}}>{DatePickerTemplate.bind({})}</Story>
</Canvas>