
import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { Dropdown } from '../../src/components/index';

<Meta
  title="MDX/Dropdown"
  component={Dropdown}
/>

export const Template = (args) => (
    <Dropdown {...args}>
  <Dropdown.Toggle variant="success" id="dropdown-basic">
    Dropdown Button
  </Dropdown.Toggle>
  <Dropdown.Menu>
    <Dropdown.Item href="#/action-1">Action</Dropdown.Item>
    <Dropdown.Item href="#/action-2">Another action</Dropdown.Item>
    <Dropdown.Item href="#/action-3">Something else</Dropdown.Item>
  </Dropdown.Menu>
</Dropdown>
  );



### Single button dropdowns

The basic Dropdown is composed of a wrapping `Dropdown` and
inner `<DropdownMenu>`, and `<DropdownToggle>`. By
default the `<DropdownToggle>` will render a
`Button` component and accepts all the same props.
The basic Dropdown is composed of a wrapping `Dropdown` and
inner `<DropdownMenu>`, and `<DropdownToggle>`. By
default the `<DropdownToggle>` will render a
`Button` component and accepts all the same props.The basic Dropdown is composed of a wrapping `Dropdown` and
inner `<DropdownMenu>`, and `<DropdownToggle>`. By
default the `<DropdownToggle>` will render a
`Button` component and accepts all the same props.The basic Dropdown is composed of a wrapping `Dropdown` and
inner `<DropdownMenu>`, and `<DropdownToggle>`. By
default the `<DropdownToggle>` will render a
`Button` component and accepts all the same props.The basic Dropdown is composed of a wrapping `Dropdown` and
inner `<DropdownMenu>`, and `<DropdownToggle>`. By
default the `<DropdownToggle>` will render a
`Button` component and accepts all the same props.The basic Dropdown is composed of a wrapping `Dropdown` and
inner `<DropdownMenu>`, and `<DropdownToggle>`. By
default the `<DropdownToggle>` will render a
`Button` component and accepts all the same props.The basic Dropdown is composed of a wrapping `Dropdown` and
inner `<DropdownMenu>`, and `<DropdownToggle>`. By
default the `<DropdownToggle>` will render a
`Button` component and accepts all the same props.The basic Dropdown is composed of a wrapping `Dropdown` and
inner `<DropdownMenu>`, and `<DropdownToggle>`. By
default the `<DropdownToggle>` will render a
`Button` component and accepts all the same props.The basic Dropdown is composed of a wrapping `Dropdown` and
inner `<DropdownMenu>`, and `<DropdownToggle>`. By
default the `<DropdownToggle>` will render a
`Button` component and accepts all the same props.The basic Dropdown is composed of a wrapping `Dropdown` and
inner `<DropdownMenu>`, and `<DropdownToggle>`. By
default the `<DropdownToggle>` will render a
`Button` component and accepts all the same props.The basic Dropdown is composed of a wrapping `Dropdown` and
inner `<DropdownMenu>`, and `<DropdownToggle>`. By
default the `<DropdownToggle>` will render a
`Button` component and accepts all the same props.The basic Dropdown is composed of a wrapping `Dropdown` and
inner `<DropdownMenu>`, and `<DropdownToggle>`. By
default the `<DropdownToggle>` will render a
`Button` component and accepts all the same props.The basic Dropdown is composed of a wrapping `Dropdown` and
inner `<DropdownMenu>`, and `<DropdownToggle>`. By
default the `<DropdownToggle>` will render a
`Button` component and accepts all the same props.The basic Dropdown is composed of a wrapping `Dropdown` and
inner `<DropdownMenu>`, and `<DropdownToggle>`. By
default the `<DropdownToggle>` will render a
`Button` component and accepts all the same props.The basic Dropdown is composed of a wrapping `Dropdown` and
inner `<DropdownMenu>`, and `<DropdownToggle>`. By
default the `<DropdownToggle>` will render a
`Button` component and accepts all the same props.The basic Dropdown is composed of a wrapping `Dropdown` and
inner `<DropdownMenu>`, and `<DropdownToggle>`. By
default the `<DropdownToggle>` will render a
`Button` component and accepts all the same props.The basic Dropdown is composed of a wrapping `Dropdown` and
inner `<DropdownMenu>`, and `<DropdownToggle>`. By
default the `<DropdownToggle>` will render a
`Button` component and accepts all the same props.The basic Dropdown is composed of a wrapping `Dropdown` and
inner `<DropdownMenu>`, and `<DropdownToggle>`. By
default the `<DropdownToggle>` will render a
`Button` component and accepts all the same props.The basic Dropdown is composed of a wrapping `Dropdown` and
inner `<DropdownMenu>`, and `<DropdownToggle>`. By
default the `<DropdownToggle>` will render a
`Button` component and accepts all the same props.The basic Dropdown is composed of a wrapping `Dropdown` and
inner `<DropdownMenu>`, and `<DropdownToggle>`. By
default the `<DropdownToggle>` will render a
`Button` component and accepts all the same props.The basic Dropdown is composed of a wrapping `Dropdown` and
inner `<DropdownMenu>`, and `<DropdownToggle>`. By
default the `<DropdownToggle>` will render a
`Button` component and accepts all the same props.The basic Dropdown is composed of a wrapping `Dropdown` and
inner `<DropdownMenu>`, and `<DropdownToggle>`. By
default the `<DropdownToggle>` will render a
`Button` component and accepts all the same props.The basic Dropdown is composed of a wrapping `Dropdown` and
inner `<DropdownMenu>`, and `<DropdownToggle>`. By
default the `<DropdownToggle>` will render a
`Button` component and accepts all the same props.The basic Dropdown is composed of a wrapping `Dropdown` and
inner `<DropdownMenu>`, and `<DropdownToggle>`. By
default the `<DropdownToggle>` will render a
`Button` component and accepts all the same props.The basic Dropdown is composed of a wrapping `Dropdown` and
inner `<DropdownMenu>`, and `<DropdownToggle>`. By
default the `<DropdownToggle>` will render a
`Button` component and accepts all the same props.

<Canvas withSource="open">
  <Story name="Dropdown">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="Dropdown"/>
