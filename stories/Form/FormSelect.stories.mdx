import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { HIDDEN_COMMON_PROPS } from '../helpers';
import { FormSelect, Button, Form } from '../../src/index';

<Meta title="Components/Form/FormSelect" component={FormSelect} />

export const templateCodeDefaultSelectMenu = `
  <Form.Group>
      <Form.Label htmlFor="defaultSelect">Default select menu</Form.Label>
      <FormSelect aria-label="Default select example" id="defaultSelect">
        <option>Open this select menu</option>
        <option value="1">One</option>
        <option value="2">Two</option>
        <option value="3">Three</option>
      </FormSelect>
</Form.Group>
`;

export const templateCodeFormSelectSizes = `
  <Form.Select size="lg">
  <option>Large select</option>
</Form.Select>
<Form.Select>
  <option>Default select</option>
</Form.Select>
<Form.Select size="sm">
  <option>Small select</option>
</Form.Select>`;

# ` <Form.Select />`

Customize the native `<select>`s with custom CSS that changes the element’s initial appearance.

export const TemplateWithLabel = (label, size) => {
  return (args) => (
    <Form.Group>
      <Form.Label htmlFor="defaultSelect">{label}</Form.Label>
      <FormSelect
        size={size}
        aria-label="Default select example"
        id="defaultSelect"
      >
        <option>Open this select menu</option>
        <option value="1">One</option>
        <option value="2">Two</option>
        <option value="3">Three</option>
      </FormSelect>
    </Form.Group>
  );
};

export const DefaultSelect = TemplateWithLabel('Default select menu');
export const LargeSelect = TemplateWithLabel('Large select menu', 'lg');
export const SmallSelect = TemplateWithLabel('Small select menu', 'sm');

<Canvas>
  <Story
    name="Basic"
    parameters={{
      docs: {
        source: {
          code: templateCodeDefaultSelectMenu,
        },
        type: 'code',
      },
    }}
  >
    {DefaultSelect.bind({})}
  </Story>
</Canvas>

## API

```jsx
import { FormSelect } from '@govtechsg/sgds-react/Form';
//or
import { Form } from '@govtechsg/sgds-react/Form';
// use <Form.Select />
```

<ArgsTable story="Basic" />

## Form Select Sizing

You may also choose from small and large custom selects to match our similarly sized text inputs.

<Canvas>
  <Story
    name="Large Select Sizing"
    parameters={{
      docs: {
        source: {
          code: templateCodeFormSelectSizes,
        },
        type: 'code',
      },
    }}
  >
    {LargeSelect.bind({})}
  </Story>
  <Story
    name="Default Select Sizing"
    parameters={{
      docs: {
        source: {
          code: '',
        },
      },
    }}
  >
    {DefaultSelect.bind({})}
  </Story>
  <Story
    name="Small Select Sizing"
    parameters={{
      docs: {
        source: {
          code: '',
        },
      },
    }}
  >
    {SmallSelect.bind({})}
  </Story>
</Canvas>
