import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import {
  Tooltip,
  Button,
  OverlayTrigger,
  Row,
  Col,
} from '../../src/components/index';
import { HIDDEN_COMMON_PROPS } from '../helpers';

<Meta
  title="MDX/SGDSTooltip"
  component={Tooltip}
  argTypes={{ ...HIDDEN_COMMON_PROPS }}
/>

export const Template = (args) => {
    return (<Tooltip {...args}/>)
}

<Canvas>
    <Story name="Default"> 
        {Template.bind({})}
    </Story>
</Canvas>

<ArgsTable story="Default" />
