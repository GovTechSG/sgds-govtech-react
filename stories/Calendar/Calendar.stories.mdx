import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { Calendar } from '../../src/components/index'
import { useState } from 'react';

<Meta title="MDX/Calendar" component={Calendar} />

export function CalendarTemplate(args) {
  const [value, onChange] = useState(new Date());
  return (
    <div>
      <Calendar
        {...args}
        onChange={onChange}
        value={value}
      />
    </div>
  );
}

<Canvas>
  <Story name="Default">{CalendarTemplate.bind({})}</Story>
</Canvas>