import { useState } from 'react';
import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { Button } from '../../src/Button/Button';
import { FileUpload } from '../../src/FileUpload/FileUpload';
import { Form } from '../../src/Form/index';
import { HIDDEN_COMMON_PROPS } from '../helpers';

<Meta title="Components/FileUpload" component={FileUpload} />

export const templateCodeFileUpload = `
const FileUpload  = () => {
  const [selectedFile, setSelectedFile] = useState({});
  const onChangeFile = (data) => {
    setSelectedFile(data);
  };
  console.log(selectedFile);
  return (
    <>
      <Form>
        <FileUpload
          controlId="fileupload1"
          onChangeFile={onChangeFile}
          selectedFile={selectedFile}
        >
          <i className="bi bi-upload me-2"></i>Choose a file
        </FileUpload>
      </Form>
    </>
  );
};
`;

export const templateCodeCustomizedIcons = `
const FileUpload  = () => {
  const [selectedFile, setSelectedFile] = useState({});
  const onChangeFile = (data) => {
    setSelectedFile(data);
  };
  console.log(selectedFile);
  return (
    <>
      <Form>
        <FileUpload
          controlId="fileupload1"
          onChangeFile={onChangeFile}
          selectedFile={selectedFile}
          cancelIcon={<i className="bi bi-x-octagon"></i>}
          checkedIcon={<i className="bi bi-check2-circle"></i>}
        >
          <i className="bi bi-upload me-2"></i>Choose a file
        </FileUpload>
      </Form>
    </>
  );
};
`;

# File Upload

`File Upload` is the process of publishing information (web pages, text, pictures, video, etc.) to a remote server. It allows uploading of a single or multiple files at any one time.

export const Template = (args) => {
  const [selectedFile, setSelectedFile] = useState({});
  const onChangeFile = (data) => {
    setSelectedFile(data);
  };
  return (
    <>  
      <Form>
        <FileUpload
          {...args}
          controlId="fileupload1"
          onChangeFile={onChangeFile}
          selectedFile={selectedFile}
        >
          <i className="bi bi-upload me-2"></i>Choose a file
        </FileUpload>
      </Form>
    </>
  );
};

<Canvas>
  <Story
    name="Basic"
    parameters={{
      docs: {
        source: {
          code: templateCodeFileUpload,
        },
        type: 'code',
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## API

```jsx
import { FileUpload } from '@govtechsg/sgds-react/FileUpload';
```

<ArgsTable story="Basic" />

## Customize the checked and cancel icons

Change up the default checked and cancel icons by passing in icons of your choice

<Canvas>
  <Story
    name="Custom Icons"
    args={{
      cancelIcon: <i className="bi bi-x-octagon"></i>,
      checkedIcon: <i className="bi bi-check2-circle"></i>,
    }}
    parameters={{
      docs: {
        source: {
          code: templateCodeCustomizedIcons,
        },
        type: 'code',
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
