import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { Typeahead } from '../../src/index';
import { useState, useRef } from 'react';

<Meta
  title="Components/Typeahead"
  component={Typeahead}
/>

export const TypeaheadTemplate = () => {
  return <Typeahead />;
};

<Canvas>
  <Story name="Default" args={{}} height="380px">
    {TypeaheadTemplate.bind({})}
  </Story>
</Canvas>
