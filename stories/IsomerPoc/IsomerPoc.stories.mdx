import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { SgdsMasthead } from '@govtechsg/sgds-masthead-react';
import '@govtechsg/sgds-masthead/dist/sgds-masthead/sgds-masthead.css';

import { Button, Col, Row, Badge } from '../../src/index';

import { useState } from 'react';

<Meta title="Isomer POC" />

# Feature Section with Overlay Images

Showcases important content elements in a visually appealing layout with overlay images

export const FeatureSectionOverlayImageTemplate = ({
  showHeadLine,
  headLine,
  displayBadge,
  badgeText,
  badgePill,
  badgeTextColor,
  badgeBg,
  title,
  text,
  showButton1,
  buttonText1,
  buttonLink1,
  buttonVariant1,
  showButton2,
  buttonText2,
  buttonLink2,
  buttonVariant2,
  image1,
  imageAlt1,
  image2,
  imageAlt2,
  imageOnRight,
}) => {
  return (
    <Row className="m-md-8 align-items-center">
      <Col
        md="12"
        lg="5"
        className={`my-3 ${imageOnRight ? 'order-lg-0' : 'order-lg-1'}`}
      >
        {showHeadLine && (
          <p className="text-primary">
            <strong>{headLine}</strong>
          </p>
        )}
        {displayBadge && (
          <Badge pill={badgePill} text={badgeTextColor} bg={badgeBg}>
            {badgeText}
          </Badge>
        )}
        <h2>{title}</h2>
        <p>{text}</p>
        <div className="tw-space-x-1">
          {showButton1 && (
            <Button
              as={buttonLink1 ? 'a' : 'button'}
              href={buttonLink1}
              variant={buttonVariant1}
            >
              {buttonText1}
            </Button>
          )}
          {showButton2 && (
            <Button
              as={buttonLink2 ? 'a' : 'button'}
              href={buttonLink2}
              variant={buttonVariant2}
            >
              {buttonText2}
            </Button>
          )}
        </div>
      </Col>
      <Col
        md="12"
        lg="7"
        className={`my-3 ${imageOnRight ? 'order-lg-1' : 'order-lg-0'}`}
      >
        <div className="tw-grid tw-grid-cols-6 tw-min-h-48 md:tw-min-h-80 lg:tw-min-h-96">
          <img
            src={image1}
            alt={imageAlt1}
            className="img-fluid tw-z-10 tw-row-start-1 tw-col-start-1 tw-col-span-4 tw-self-end tw-h-auto tw-w-full tw-drop-shadow-[0_0_0.9rem_var(--sgds-gray-300)]"
          />
          <img
            src={image2}
            alt={imageAlt2}
            className="img-fluid tw-row-start-1 tw-col-start-2 tw-col-end-7 tw-h-auto tw-w-full tw-drop-shadow-[0_0_0.9rem_var(--sgds-gray-300)]"
          />
        </div>
      </Col>
    </Row>
  );
};

export const buttonVariantOption = [
  'primary',
  'secondary',
  'success',
  'danger',
  'warning',
  'info',
  'dark',
  'light',
  'link',
  'outline-primary',
  'outline-secondary',
  'outline-success',
  'outline-danger',
  'outline-warning',
  'outline-info',
  'outline-dark',
  'outline-light',
];

<Canvas>
  <Story
    name="Feature Section with Overlay Images"
    args={{
      showHeadLine: true,
      headLine: 'Save time with our plug-n-play components',
      displayBadge: true,
      badgeText: 'sgds 2.2.0',
      badgePill: false,
      badgeTextColor: 'white',
      badgeBg: 'secondary',
      title: 'Build your next best digital experience for Singapore',
      text: 'The Singapore Government Design System was developed to empower teams in creating fast, accessible and mobile-friendly digital services.',
      showButton1: true,
      buttonText1: 'Get Started',
      buttonLink1: '',
      buttonVariant1: 'primary',
      showButton2: true,
      buttonText2: 'Install SGDS',
      buttonLink2: '',
      buttonVariant2: 'outline-dark',
      image1:
        'https://www.designsystem.tech.gov.sg/assets/img/landing-hero/sgds.png',
      imageAlt1: '',
      image2:
        'https://www.designsystem.tech.gov.sg/assets/img/landing-hero/fundamental%20icons.png',
      imageAlt2: '',
      imageOnRight: true,
    }}
    argTypes={{
      badgeTextColor: {
        options: [
          'muted',
          'dark',
          'primary',
          'secondary',
          'success',
          'danger',
          'warning',
          'info',
          'light',
          'white',
        ],
        control: {
          type: 'select',
        },
      },
      badgeBg: {
        options: [
          'primary',
          'secondary',
          'success',
          'danger',
          'warning',
          'info',
          'light',
          'dark',
        ],
        control: {
          type: 'select',
        },
      },
      buttonVariant1: {
        options: buttonVariantOption,
        control: {
          type: 'select',
        },
      },
      buttonVariant2: {
        options: buttonVariantOption,
        control: {
          type: 'select',
        },
      },
      validated: {
        table: {
          disable: true,
        },
      },
      as: {
        table: {
          disable: true,
        },
      },
    }}
  >
    {FeatureSectionOverlayImageTemplate.bind({})}
  </Story>
</Canvas>

# Feature Section with Single Image

Showcases important content elements in a visually appealing layout with single image

export const FeatureSectionSingleImageTemplate = ({
  showHeadLine,
  headLine,
  displayBadge,
  badgeText,
  badgePill,
  badgeTextColor,
  badgeBg,
  title,
  text,
  showButton1,
  buttonText1,
  buttonLink1,
  buttonVariant1,
  showButton2,
  buttonText2,
  buttonLink2,
  buttonVariant2,
  image,
  imageAlt,
  imageOnRight,
}) => {
  return (
    <Row className="m-md-8 align-items-center">
      <Col
        md="12"
        lg="5"
        className={`my-3 ${imageOnRight ? 'order-lg-0' : 'order-lg-1'}`}
      >
        {showHeadLine && (
          <p className="text-primary">
            <strong>{headLine}</strong>
          </p>
        )}
        {displayBadge && (
          <Badge pill={badgePill} text={badgeTextColor} bg={badgeBg}>
            {badgeText}
          </Badge>
        )}
        <h2>{title}</h2>
        <p>{text}</p>
        <div className="tw-space-x-1">
          {showButton1 && (
            <Button
              as={buttonLink1 ? 'a' : 'button'}
              href={buttonLink1}
              variant={buttonVariant1}
            >
              {buttonText1}
            </Button>
          )}
          {showButton2 && (
            <Button
              as={buttonLink2 ? 'a' : 'button'}
              href={buttonLink2}
              variant={buttonVariant2}
            >
              {buttonText2}
            </Button>
          )}
        </div>
      </Col>
      <Col
        md="12"
        lg="7"
        className={`my-3 ${imageOnRight ? 'order-lg-1' : 'order-lg-0'}`}
      >
        <img
          src={image}
          alt={imageAlt}
          className="img-fluid tw-h-auto tw-w-full tw-drop-shadow-[0_0_0.9rem_var(--sgds-gray-300)]"
        />
      </Col>
    </Row>
  );
};

<Canvas>
  <Story
    name="Feature Section with Single Image"
    args={{
      showHeadLine: true,
      headLine: 'Common design elements for consistency',
      displayBadge: true,
      badgeText: 'sgds 2.2.0',
      badgePill: false,
      badgeTextColor: 'white',
      badgeBg: 'secondary',
      title: 'Boost user confidence with our recognisable design patterns',
      text: "By using standard text styles, icons and interface elements, consistency is achieved. This strengthens users' intuition when interacting with different digital services.",
      showButton1: true,
      buttonText1: 'Get Started',
      buttonLink1: '',
      buttonVariant1: 'primary',
      showButton2: true,
      buttonText2: 'Install SGDS',
      buttonLink2: '',
      buttonVariant2: 'outline-dark',
      image:
        'https://www.designsystem.tech.gov.sg/assets/img/landing-hero/part-of-change-2.png',
      imageAlt: '',
      imageOnRight: true,
    }}
    argTypes={{
      badgeTextColor: {
        options: [
          'muted',
          'dark',
          'primary',
          'secondary',
          'success',
          'danger',
          'warning',
          'info',
          'light',
          'white',
        ],
        control: {
          type: 'select',
        },
      },
      badgeBg: {
        options: [
          'primary',
          'secondary',
          'success',
          'danger',
          'warning',
          'info',
          'light',
          'dark',
        ],
        control: {
          type: 'select',
        },
      },
      buttonVariant1: {
        options: buttonVariantOption,
        control: {
          type: 'select',
        },
      },
      buttonVariant2: {
        options: buttonVariantOption,
        control: {
          type: 'select',
        },
      },
      validated: {
        table: {
          disable: true,
        },
      },
      as: {
        table: {
          disable: true,
        },
      },
    }}
  >
    {FeatureSectionSingleImageTemplate.bind({})}
  </Story>
</Canvas>
