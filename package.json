{
  "name": "sgds-govtech-react",
  "version": "0.1.1",
  "files": [
    "dist"
  ],
  "main": "dist/index.js",
  "scripts": {
    "build": "react-scripts build",
    "compile": "rm -rf ./dist && NODE_ENV=production babel src/components --out-dir ./dist --source-maps",
    "compile::watch": "rm -rf ./dist && NODE_ENV=production babel src/components --watch --out-dir ./dist --source-maps",
    "eject": "react-scripts eject",
    "start": "react-scripts start",
    "test": "react-scripts test"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "devDependencies": {
    "@babel/cli": "^7.5.5",
    "node-sass": "^4.12.0",
    "prettier": "^1.18.2",
    "prop-types": "^15.7.2",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "3.0.0",
    "react-syntax-highlighter": "^11.0.2",
    "sgds-govtech": "^1.2.3",
    "styled-components": "^4.3.2"
  }
}
